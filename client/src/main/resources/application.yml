server:
  port: 8080

spring:
  application:
    name: CLIENT-SERVICE


management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
